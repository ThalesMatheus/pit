import * as React from 'react'
import group_entry from 
export const confirm_button = (groupid: any, uuid: any) => {
        return (
     <button
     onClick={group_entry}
     >
        
     </button>
    )
}
